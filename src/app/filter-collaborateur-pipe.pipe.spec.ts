import { FilterCollaborateurPipePipe } from './filter-collaborateur-pipe.pipe';

describe('FilterCollaborateurPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCollaborateurPipePipe();
    expect(pipe).toBeTruthy();
  });
});
